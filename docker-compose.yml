version: "3.8"

services:
  bot:
    build: .
    container_name: birthday-bot
    restart: always
    ports:
      - "3000:3000"
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - BASE_URL=${BASE_URL}
      - PORT=3000